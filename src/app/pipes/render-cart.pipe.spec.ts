import { RenderCartPipe } from './render-cart.pipe';

describe('RenderCartPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderCartPipe();
    expect(pipe).toBeTruthy();
  });
});
